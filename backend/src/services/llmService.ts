export const explain = async (
  code: string,
  complexity: string
): Promise<string> => {
  // Placeholder for real LLM call

  return `The estimated complexity is ${complexity}. Consider optimizing nested loops if present.`;
};
